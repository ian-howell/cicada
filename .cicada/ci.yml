name: ci
image: golang:latest
on:
  - push
  - pull_request
steps:
  - name: vet
    commands:
      - go vet ./...
  - name: test
    commands:
      - go test ./...
    dependsOn:
      - vet
  - name: build
    commands:
      - go build ./cmd/cicada
    dependsOn:
      - test
